# frozen_string_literal: true

source "https://rubygems.org"

# Specify the Ruby version
ruby "3.0.0" # Use the version you are working with; this is an example

# Jekyll and GitHub Pages
gem "github-pages", group: :jekyll_plugins

# Additional Jekyll plugins you might use
gem "jekyll-sitemap" # Generates a sitemap.xml
gem "jekyll-seo-tag" # Adds SEO tags to your site
gem "jekyll-feed"    # Generates an RSS feed
gem "jekyll-paginate" # Pagination for posts
gem "jekyll-gist"    # Adds support for Gist embedding

# Uncomment if you are using SCSS
# gem "jekyll-sass-converter"

# Optional: If you need any other gems, add them here
